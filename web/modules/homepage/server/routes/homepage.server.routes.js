'use strict';

/**
 * Module dependencies
 */
var homepagePolicy = require('../policies/homepage.server.policy'),
  homepage = require('../controllers/homepage.server.controller');

module.exports = function (app) {
  
  
};
